# Datasets

Sample datasets and user-provided uploads for RelatAI live here.
The `samples/` directory contains curated datasets for testing and demos.
User uploads should be stored in `uploads/` (ignored via .gitignore).
